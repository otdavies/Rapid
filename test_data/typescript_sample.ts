// This is a file-level comment.

/**
 * This is a JSDoc comment for a class.
 */
class MyClass {
    // This is a comment for a method.
    myMethod(arg1: string, arg2: number): void {
        console.log(arg1, arg2);
    }
}

/*
    This is a multi-line comment for a function.
*/
function myFunction(): void {
    // This is a comment inside a function.
}
